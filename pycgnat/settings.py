# Define the supported platforms.
# - Uses the same name defined by the file inside 'generator' package
# - Is used for auto calling the 'generate' function at runtime execution
SUPPORTED_PLATFORMS = ("routeros",)
